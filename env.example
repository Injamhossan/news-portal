# MongoDB Connection URI (Used by server)
MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/doiniksorboshesh?retryWrites=true&w=majority

# API Base URL (Used by client)
# Leave empty for same-domain deployment (e.g. Vercel)
# Set to absolute URL for separate backend (e.g. https://api.mysite.com)
NEXT_PUBLIC_API_URL=
